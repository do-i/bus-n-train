<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="org.djd.busntrain"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-sdk android:minSdkVersion="10"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name">
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".bus.BusRouteActivity">
        </activity>
        <activity android:name=".bus.BusDirectionActivity">
        </activity>
        <activity android:name=".bus.BusStopActivity">
        </activity>

        <activity android:name=".bus.BusPredictionActivity"></activity>
        <activity android:name=".bus.BusFavoriteActivity"></activity>
        <service android:name=".bus.BusRouteService">
        </service>
        <service android:name=".bus.BusDirectionService">
        </service>
        <service android:name=".bus.BusStopService">
        </service>

        <service android:name=".bus.BusPredictionService"></service>
        <provider android:name=".provider.BusRouteContentProvider"
                  android:authorities="org.djd.busntrain.provider.busroutecontentprovider"></provider>
        <provider android:name=".provider.BusFavoriteContentProvider"
                  android:authorities="org.djd.busntrain.provider.busfavoritecontentprovider"></provider>

    </application>

</manifest>